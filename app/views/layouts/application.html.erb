<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Yoleas</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>

<header>
  <h1>Yoleas</h1>
  <h2>Something about it</h2>
</header>
<%= yield %>

<footer>
  <%= link_to 'Home', root_path %>
  <% if signed_in? %>| <%= link_to 'My Info', edit_user_path(current_user) %>
      | Logged in as <%= current_user.full_name %>
      | <%= link_to 'Sign Out', session_path, :method => :delete %>
  <% end %>
</footer>
</body>
</html>
